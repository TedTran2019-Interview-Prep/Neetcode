# @param {Integer[]} nums
# @return {Integer[]}
# answer[i] = rest of elements multiplied together, except self
# If division were allowed, can just run through array twice.
# Once to get total multiplication, then divide total by self for each element.
# 
def product_except_self(nums)
    
end